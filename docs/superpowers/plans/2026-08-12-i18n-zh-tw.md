# 前端 i18n（vue-i18n）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 前端所有固定 user-visible 字串語系化（vue-i18n Composition API），預設 zh-TW，建立完整 en locale，動態原始資料不翻譯。

**Architecture:** `vue-i18n ^11.4.8`（`legacy:false`），`i18n/index.ts` createI18n + export `i18n` 實例；元件用 `useI18n().t`、store/TS 模組用 `i18n.global.t`；三份 raw→key 狀態映射（session/gate/codex tool）＋ unknown passthrough；zh-TW.ts 為 runtime 權威。

**Tech Stack:** Vue 3.5、Pinia、vue-i18n 11、vitest、@vue/test-utils、Wails v2。

**Spec:** `docs/superpowers/specs/2026-08-12-i18n-zh-tw-design.md`（rev, closure PASS）。譯文審核來源 `docs/i18n-zh-TW.md`。

## Global Constraints

- **Composition API**：`createI18n({legacy:false, locale:'zh-TW', fallbackLocale:'en', messages:{'zh-TW':zhTW, en}})`；元件 `useI18n()`、非元件 `i18n.global.t()`。
- **範圍**：所有固定 UI 字串（按鈕/tab/placeholder/title/空狀態/前端錯誤前綴/store 固定訊息）雙 locale；**不翻**：後端原始錯誤內容、provider 名、wire 值（on-request/never/untrusted）、事件 kind、資料契約欄位**值**與 bindings kind、`Gherkin`/`oracle`/`session`/`tokens`/`commit`。
- **key**：語意分層、非元件位置、非英文原文。
- **狀態映射**：三份獨立 raw→key（`sessionStateKeys`/`gateStateKeys`/`codexToolStatusKeys`）；**unknown 值原樣顯示**（不顯示缺漏 key）。
- **interpolation**：`{action}`/`{error}`/`{status}` 組完整句子；標點在 locale 內。
- **引用正確性**：**literal `t('...')` scan 測試**（誤 key 即失敗）——owner 定案，不用 TS union。
- **source of truth**：`zh-TW.ts` runtime 權威；`i18n-zh-TW.md` 審核快照。
- **最終 gate**：vitest 全綠、`npm run build`（vue-tsc）、**`wails build`**。

---

## File Structure

**新增：**
- `frontend/src/i18n/index.ts` — createI18n + `export const i18n`、`export const t = i18n.global.t`。
- `frontend/src/i18n/locales/zh-TW.ts`、`frontend/src/i18n/locales/en.ts` — 訊息物件。
- `frontend/src/i18n/stateKeys.ts` — 三份 raw→key 映射 ＋ `resolveState(map, raw)` passthrough helper。
- `frontend/src/test/i18n.ts` — `mountWithI18n()` test factory。
- `frontend/src/i18n/locales.parity.test.ts`、`frontend/src/i18n/keyRefs.test.ts`、`frontend/src/i18n/stateKeys.test.ts` — 驗證測試。

**修改：** `frontend/package.json`（加 vue-i18n）、`frontend/src/main.ts`（掛 plugin）、所有元件、`frontend/src/stores/session.ts`、既有元件測試（接 mountWithI18n）。

---

## Task 1: vue-i18n 建置 + skeleton

**Files:**
- Modify: `frontend/package.json`, `frontend/src/main.ts`
- Create: `frontend/src/i18n/index.ts`, `frontend/src/i18n/locales/zh-TW.ts`, `frontend/src/i18n/locales/en.ts`
- Test: `frontend/src/i18n/setup.test.ts`

**Interfaces:**
- Produces: `i18n`（vue-i18n 實例）、`export const t = i18n.global.t`（非元件用）、`createI18nForTest()`（Task 8 用，可先在此 export 或 Task 8 建）。

- [ ] **Step 1: 安裝 vue-i18n**

Run: `cd frontend && npm install vue-i18n@^11.4.8`
Expected: package.json/package-lock 更新，無 peer 錯誤（Vue 3.5 相容）。

- [ ] **Step 2: 建 locale skeleton（先放一個 key 讓後續擴充）**

```ts
// frontend/src/i18n/locales/zh-TW.ts
export default {
  app: { tab: { chat: '對話' } },
} as const

// frontend/src/i18n/locales/en.ts
export default {
  app: { tab: { chat: 'Chat' } },
}
```

- [ ] **Step 3: createI18n（Composition API）**

```ts
// frontend/src/i18n/index.ts
import { createI18n } from 'vue-i18n'
import zhTW from './locales/zh-TW'
import en from './locales/en'

export const i18n = createI18n({
  legacy: false,
  locale: 'zh-TW',
  fallbackLocale: 'en',
  messages: { 'zh-TW': zhTW, en },
})

// 非元件（store／一般 TS 模組）產生固定 UI 訊息用這支
export const t = i18n.global.t
```

- [ ] **Step 4: main.ts 掛載**

在 `frontend/src/main.ts` `createApp(App)` 之後、`.mount` 之前加 `app.use(i18n)`（import from `./i18n`）。

- [ ] **Step 5: 寫 smoke 測試**

```ts
// frontend/src/i18n/setup.test.ts
import { describe, it, expect } from 'vitest'
import { i18n } from './index'

describe('i18n setup', () => {
  it('defaults to zh-TW and resolves a key', () => {
    expect(i18n.global.locale.value).toBe('zh-TW')
    expect(i18n.global.t('app.tab.chat')).toBe('對話')
  })
  it('falls back to en for a zh-only-missing key path via fallbackLocale', () => {
    expect(i18n.global.fallbackLocale.value).toBe('en')
  })
})
```

- [ ] **Step 6: 驗證 + commit**

Run: `cd frontend && npx vitest run src/i18n/setup.test.ts && npm run build`
Expected: PASS、build 乾淨。
```bash
git add frontend/package.json frontend/package-lock.json frontend/src/i18n/ frontend/src/main.ts
git commit -m "feat(i18n): vue-i18n Composition API setup + locale skeleton"
```

---

## Task 2: stateKeys 三映射 + passthrough helper

**Files:**
- Create: `frontend/src/i18n/stateKeys.ts`
- Test: `frontend/src/i18n/stateKeys.test.ts`

**Interfaces:**
- Produces:
  - `sessionStateKeys`、`gateStateKeys`、`codexToolStatusKeys`（`Record<string,string>` const）
  - `function resolveState(map: Record<string,string>, raw: string, translate: (k:string)=>string): string` — 已知 raw → `translate(map[raw])`；未知 raw → 原樣回 `raw`（**不回缺漏 key**）。

- [ ] **Step 1: Write the failing test**

```ts
import { describe, it, expect } from 'vitest'
import { sessionStateKeys, gateStateKeys, codexToolStatusKeys, resolveState } from './stateKeys'

const fakeT = (k: string) => `T(${k})`

describe('stateKeys', () => {
  it('three maps are independent and cover known raws', () => {
    expect(sessionStateKeys.idle).toBe('session.state.idle')
    expect(gateStateKeys.stale).toBe('gate.state.stale')
    expect(codexToolStatusKeys.inProgress).toBe('timeline.toolStatus.inProgress')
  })
  it('resolveState translates known raw', () => {
    expect(resolveState(sessionStateKeys, 'idle', fakeT)).toBe('T(session.state.idle)')
  })
  it('resolveState passes through unknown raw (no missing key leak)', () => {
    expect(resolveState(sessionStateKeys, 'weird_state', fakeT)).toBe('weird_state')
    expect(resolveState(gateStateKeys, 'bogus', fakeT)).toBe('bogus')
  })
})
```

- [ ] **Step 2: Run to verify fail** — `npx vitest run src/i18n/stateKeys.test.ts` → FAIL。

- [ ] **Step 3: Implement**

```ts
// frontend/src/i18n/stateKeys.ts
export const sessionStateKeys: Record<string, string> = {
  idle: 'session.state.idle', waiting: 'session.state.waiting',
  streaming: 'session.state.streaming', tool_running: 'session.state.toolRunning',
  awaiting_approval: 'session.state.awaitingApproval', retrying: 'session.state.retrying',
  done: 'session.state.done', failed: 'session.state.failed',
}
export const gateStateKeys: Record<string, string> = {
  pending: 'gate.state.pending', active: 'gate.state.active',
  stale: 'gate.state.stale', superseded: 'gate.state.superseded',
}
export const codexToolStatusKeys: Record<string, string> = {
  completed: 'timeline.toolStatus.completed', inProgress: 'timeline.toolStatus.inProgress',
  failed: 'timeline.toolStatus.failed',
}
export function resolveState(map: Record<string, string>, raw: string, translate: (k: string) => string): string {
  const key = map[raw]
  return key ? translate(key) : raw // unknown → 原樣，不洩漏缺漏 key
}
```

- [ ] **Step 4: Run** — PASS。
- [ ] **Step 5: Commit** — `feat(i18n): raw-state to key maps with unknown passthrough`

---

## Task 3: 完整 locale 檔（zh-TW + en）

**Files:**
- Modify: `frontend/src/i18n/locales/zh-TW.ts`, `frontend/src/i18n/locales/en.ts`
- Test: （由 Task 9 的 parity/keyRefs 涵蓋；本 task 只填內容，跑 build 確保型別 OK）

**Interfaces:**
- Produces: 完整 message 樹（下方 inventory）。**兩檔 leaf path 必須完全一致**（Task 9 parity 測試守）。

**完整 key inventory**（zh-TW / en；en 用自然英文，不必等於舊硬編字面）：

```
app.tab.chat            對話 / Chat
app.tab.preview         預覽 / Preview
app.tab.spec            規格 / Spec
app.tab.diagram         表示圖 / Diagram
app.timeline.label      執行時間軸 / Timeline
app.resize.width        拖曳調整寬度 / Drag to resize width
app.resize.height       拖曳調整高度 / Drag to resize height
app.startupError        啟動：{error} / startup: {error}

settings.action.new         開新對話 / New
settings.action.terminate   強制終止 / Terminate
settings.action.end         結束 / End
settings.action.authStatus  登入狀態 / Auth
settings.action.login       登入 / Login
settings.action.cancelLogin 取消登入 / Cancel
settings.action.logout      登出 / Logout
settings.action.b1Probe     B1 / B1
settings.operationAction.new         開新對話 / new session
settings.operationAction.terminate   強制終止 / terminate
settings.operationAction.end         結束對話 / end session
settings.operationAction.authStatus  查詢登入狀態 / auth status query
settings.operationAction.login       登入 / login
settings.operationAction.cancelLogin 取消登入 / cancel login
settings.operationAction.logout      登出 / logout
settings.operationAction.b1Probe     B1 探測 / B1 probe
settings.operation.success  {action}成功 / {action} ok
settings.operation.failure  {action}失敗：{error} / {action} failed: {error}
settings.taskId.placeholder      任務標籤（task id） / Task label (task id)
settings.recordCase.placeholder  {provider}-case（錄流，可空） / {provider}-case (recording, optional)
settings.resumeId.placeholder    resume id（可空） / resume id (optional)
settings.approvalPolicy.tooltip     codex approvalPolicy / codex approvalPolicy
settings.approvalPolicy.untrusted   untrusted（每次核可） / untrusted (approve each)
settings.approvalPolicy.onRequest   on-request / on-request
settings.approvalPolicy.never       never（不核可，風險自負） / never (no approval, at your own risk)
settings.newSession.tooltip  結束目前 session，等待舊 provider 收尾後開新對話 / End current session, wait for the old provider to wind down, then start a new chat
settings.awaitingApproval.tooltip  等待核可 / Awaiting approval

chat.thinking            思考過程 / thinking
chat.input.placeholder   輸入訊息，Enter 送出（Shift+Enter 換行） / Type a message, Enter to send (Shift+Enter for newline)
chat.action.send         送出 / Send

gate.action.approve      核可 / Approve
gate.action.reject       退回 / Reject
gate.state.pending       待核可 / PENDING
gate.state.active        已生效 / ACTIVE
gate.state.stale         已失效 / STALE
gate.state.superseded    已取代 / SUPERSEDED
gate.reason.placeholder  理由（退回時必填） / Reason (required to reject)
gate.reasonHint          請先填理由再退回 / Enter a reason before rejecting
gate.degradedNotice      核可記錄異常：核可與退回功能已暫停，目前僅供查看 / Approval journal degraded: approve/reject paused, view only
gate.empty               目前沒有 Gate 1 項目 / No Gate 1 items
gate.label.approvalId    核可編號（approval_id） / approval_id
gate.label.baseCommit    基準 commit（base_commit） / base_commit
gate.label.specManifest  規格 manifest（spec_manifest） / spec_manifest

spec.action.draftGherkin     草擬 Gherkin / Draft Gherkin
spec.action.detectAmbiguity  歧義偵測 / Detect ambiguity
spec.action.checkOracle      oracle 覆蓋檢查 / Oracle coverage check
spec.assist.drafting         AI 產生中… / Generating…
spec.action.acceptDraft      套用草稿 / Accept
spec.action.submit           送核 / Submit for Approval
spec.action.previewCommit    預覽 commit / Preview commit
spec.action.confirmCommit    建立 commit / Confirm commit
spec.commitMessage.placeholder  commit 訊息 / commit message

approval.action.allow    允許 / Allow
approval.action.deny     拒絕 / Deny
approval.reason.placeholder  理由（拒絕時建議填寫） / Reason (suggested when denying)

diagram.empty            尚無可顯示的圖表 / No diagram to display
preview.empty            在左側選擇檔案以預覽 / Select a file on the left to preview

session.state.idle              待命 / idle
session.state.waiting           等待回覆 / waiting
session.state.streaming         回覆中 / streaming
session.state.toolRunning       工具執行中 / tool running
session.state.awaitingApproval  等待核可 / awaiting approval
session.state.retrying          重試中 / retrying
session.state.done              完成 / done
session.state.failed            失敗 / failed

statusbar.task           任務：{id} / Task: {id}
statusbar.session        session：{id} / session: {id}
statusbar.usage.providerLatest  provider 最新回報值 / provider latest
statusbar.usage.sessionTotal    本 session 累計 / session total

timeline.result.completed    完成 / ok
timeline.result.failed       失敗 / ERROR
timeline.toolStatus.completed   已完成 / completed
timeline.toolStatus.inProgress  執行中 / in progress
timeline.toolStatus.failed      失敗 / failed
timeline.summary.toolCall        工具呼叫 / tool call
timeline.summary.approvalRequest 核可請求：{text} / Approval request: {text}
timeline.summary.approvalDecision 核可決定：{text} / Approval decision: {text}
timeline.summary.stateChange     狀態 → {state} / State → {state}
timeline.summary.retry           provider 重試 / provider retry
timeline.summary.toolResult      工具結果 / tool result
timeline.raw                     原始資料 / raw

store.bindingsNotReady   綁定尚未就緒 / bindings not ready
```

- [ ] **Step 1: 依上表填 `zh-TW.ts` 與 `en.ts`**（巢狀物件；兩檔結構完全對齊）。`{action}`/`{error}`/`{status}`/`{id}`/`{provider}`/`{text}`/`{state}` 保留為 vue-i18n named interpolation。
- [ ] **Step 2: 驗證** — `cd frontend && npm run build`（vue-tsc）乾淨；`npx vitest run src/i18n/setup.test.ts` 仍綠。
- [ ] **Step 3: Commit** — `feat(i18n): complete zh-TW + en locale messages`

> 註：inventory 完整性由 Task 10 的人工掃描 + Task 9 的 keyRefs 測試把關；此表若實作時發現漏字串，補進本檔並在 Task 10 記錄。

---

## Task 4: 抽 App.vue + SettingsBar（含 8 操作×成敗）

**Files:** Modify `frontend/src/App.vue`, `frontend/src/components/SettingsBar.vue`; Test: 更新 `SettingsBar.test.ts`（Task 8 的 mountWithI18n 完成後；本 task 先確保 build 綠）。

**Interfaces:** Consumes Task 3 keys。

- [ ] **Step 1: App.vue** — `const { t } = useI18n()`；把 tab 文字（Chat/Preview/Spec/表示圖）、`▾/▸ Timeline`、兩個 resize `title`、`startup:` 前綴改 `t('app.tab.chat')` 等；`startup:` 用 `t('app.startupError', { error: cliInfo.startupError })`。
- [ ] **Step 2: SettingsBar.vue** — `useI18n()`；按鈕文字用 `t('settings.action.*')`；placeholder/tooltip 用對應 key（`recordCase` 用 `t('settings.recordCase.placeholder', { provider: s.provider })`）；approvalPolicy 選項用 `t('settings.approvalPolicy.*')`。
- [ ] **Step 3: SettingsBar 操作結果（8 操作×成敗，凍結）** — 找到 `s.note(\`${label} ok...\`)`（成功）與對應失敗 catch。改為：成功 `s.note(t('settings.operation.success', { action: t('settings.operationAction.' + opKey) }))`；失敗 `s.note(t('settings.operation.failure', { action: t('settings.operationAction.' + opKey), error: String(err) }))`。opKey ∈ {new, terminate, end, authStatus, login, cancelLogin, logout, b1Probe}。確認 8 操作皆覆蓋成功與失敗兩路徑。
- [ ] **Step 4: 驗證** — `npm run build` 乾淨。
- [ ] **Step 5: Commit** — `feat(i18n): localize App.vue tabs + SettingsBar actions/operations`

---

## Task 5: 抽 GateConsole / SpecWorkspace / ApprovalDialog / ChatPanel / DiagramPane / PreviewPane

**Files:** Modify 這 6 個元件。

- [ ] **Step 1:** 各元件 `const { t } = useI18n()`，把下列硬編字串換成 `t()`：
  - GateConsole：`Approve`/`Reject`→`gate.action.*`；badge `e.state.toUpperCase()` → `resolveState(gateStateKeys, e.state, t)`（import Task 2）；`理由（reject 必填）`→`gate.reason.placeholder`；degraded 通知→`gate.degradedNotice`；`目前沒有 Gate 1 項目`→`gate.empty`；`請先填理由再駁回`→`gate.reasonHint`；bindings 標籤（若主畫面顯示）→`gate.label.*`。
  - SpecWorkspace：三 assist 按鈕、`AI 產生中…`、`Accept`、`Submit for Approval`、`Preview Commit`、`Confirm Commit`、`commit message` placeholder → `spec.*`。
  - ApprovalDialog：`Allow`/`Deny`→`approval.action.*`；placeholder→`approval.reason.placeholder`。
  - ChatPanel：`thinking`→`chat.thinking`；input placeholder→`chat.input.placeholder`；`送出`→`chat.action.send`。
  - DiagramPane：`尚無可顯示的圖表`→`diagram.empty`。
  - PreviewPane：`在左側選擇檔案以預覽`→`preview.empty`。
- [ ] **Step 2: 驗證** — `npm run build` 乾淨。
- [ ] **Step 3: Commit** — `feat(i18n): localize gate/spec/approval/chat/diagram/preview components`

---

## Task 6: 抽 StatusBar + Timeline（狀態映射）

**Files:** Modify `frontend/src/components/StatusBar.vue`, `frontend/src/components/Timeline.vue`。

- [ ] **Step 1: StatusBar** — 刪除本地 `stateLabel` 物件；改 `resolveState(sessionStateKeys, s.state, t)`；`任務：`/`session：` 用 `t('statusbar.task', {id})` / `t('statusbar.session', {id})`；usage tooltip 用 `t('statusbar.usage.providerLatest'|'sessionTotal')`。（`tokens`/`session` 字面保留英文——已在 locale 值內。）
- [ ] **Step 2: Timeline `summary()`** — `狀態 → ${e.state}` → `t('timeline.summary.stateChange', { state: resolveState(sessionStateKeys, e.state, t) })`；`ERROR`/`ok` → `t('timeline.result.failed'|'completed')`；codex tool status → `resolveState(codexToolStatusKeys, status, t)`；`工具呼叫`/`核可請求：`/`核可決定：`/`provider 重試`/`工具結果` → `timeline.summary.*`；`raw` → `timeline.raw`。unknown state/status 走 `resolveState` 的 passthrough。
- [ ] **Step 3: 驗證** — `npm run build` 乾淨。
- [ ] **Step 4: Commit** — `feat(i18n): localize StatusBar + Timeline via shared state-key maps`

---

## Task 7: session.ts store（非元件入口）

**Files:** Modify `frontend/src/stores/session.ts`。

- [ ] **Step 1:** import `{ t }` from `../i18n`（`i18n.global.t`）；把 store 產生的**固定 UI 訊息**（如 `bindings not ready`）改 `t('store.bindingsNotReady')`。**後端／provider 回傳的動態錯誤內容維持原樣**（只有固定前綴／固定訊息翻譯）。
- [ ] **Step 2: 驗證** — `npm run build` 乾淨；既有 `session.test.ts` 若斷言該訊息字面，改斷言 `t('store.bindingsNotReady')` 或用 mountWithI18n 前的 i18n 實例。
- [ ] **Step 3: Commit** — `feat(i18n): route session store fixed messages through i18n.global.t`

---

## Task 8: mountWithI18n test factory + 既有元件測試接線

**Files:** Create `frontend/src/test/i18n.ts`; Modify 既有元件測試（GateConsole/SpecWorkspace/ChatPanel/SettingsBar/ApprovalDialog… 凡 mount 含 `useI18n()` 的元件）。

**Interfaces:**
- Produces: `function mountWithI18n(component, options?)` — 建**隔離** i18n 實例（`createI18n({legacy:false, locale:'zh-TW', fallbackLocale:'en', messages})`）並注入 `global.plugins`，回 `mount(component, {...options, global:{plugins:[i18n], ...options.global}})`。

- [ ] **Step 1: 建 factory**

```ts
// frontend/src/test/i18n.ts
import { mount } from '@vue/test-utils'
import { createI18n } from 'vue-i18n'
import zhTW from '../i18n/locales/zh-TW'
import en from '../i18n/locales/en'

export function makeI18n(locale: 'zh-TW' | 'en' = 'zh-TW') {
  return createI18n({ legacy: false, locale, fallbackLocale: 'en', messages: { 'zh-TW': zhTW, en } })
}
export function mountWithI18n(component: any, options: any = {}, locale: 'zh-TW' | 'en' = 'zh-TW') {
  const i18n = makeI18n(locale)
  return mount(component, {
    ...options,
    global: { ...(options.global ?? {}), plugins: [i18n, ...((options.global?.plugins) ?? [])] },
  })
}
```

- [ ] **Step 2:** 把既有元件測試的 `mount(X, ...)` 改成 `mountWithI18n(X, ...)`（凡該元件用了 `useI18n()`）。斷言中原本比對英文/中文字面的，改比對對應 locale 的譯文（zh-TW 預設）。
- [ ] **Step 3: 驗證** — `cd frontend && npx vitest run` 全綠（既有測試在 i18n 下通過）。
- [ ] **Step 4: Commit** — `test(i18n): mountWithI18n factory + adapt existing component tests`

---

## Task 9: 驗證測試（parity / literal scan / state maps / en render）

**Files:** Create `frontend/src/i18n/locales.parity.test.ts`, `frontend/src/i18n/keyRefs.test.ts`, `frontend/src/i18n/enRender.test.ts`（stateKeys.test.ts 已於 Task 2）。

- [ ] **Step 1: parity（遞迴 leaf + 型別一致）**

```ts
// locales.parity.test.ts
import { describe, it, expect } from 'vitest'
import zhTW from './locales/zh-TW'
import en from './locales/en'

function leaves(obj: any, prefix = ''): Record<string, 'string' | 'object'> {
  const out: Record<string, 'string' | 'object'> = {}
  for (const [k, v] of Object.entries(obj)) {
    const p = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object') { out[p] = 'object'; Object.assign(out, leaves(v, p)) }
    else out[p] = 'string'
  }
  return out
}
describe('locale parity', () => {
  it('zh-TW and en have identical leaf paths and types', () => {
    const a = leaves(zhTW), b = leaves(en)
    expect(Object.keys(a).sort()).toEqual(Object.keys(b).sort())
    for (const k of Object.keys(a)) expect(b[k]).toBe(a[k]) // 型別一致
  })
})
```

- [ ] **Step 2: literal `t()` key scan（引用正確性，方案 b）**

```ts
// keyRefs.test.ts — 掃 src 下所有 t('...') / i18n.global.t('...') literal，斷言存在於 leaf paths
import { describe, it, expect } from 'vitest'
import { readFileSync, readdirSync, statSync } from 'node:fs'
import { join } from 'node:path'
import zhTW from './locales/zh-TW'

function leafSet(obj: any, prefix = '', out = new Set<string>()): Set<string> {
  for (const [k, v] of Object.entries(obj)) {
    const p = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object') leafSet(v, p, out); else out.add(p)
  }
  return out
}
function walk(dir: string, acc: string[] = []): string[] {
  for (const e of readdirSync(dir)) {
    const f = join(dir, e)
    if (statSync(f).isDirectory()) walk(f, acc)
    else if (/\.(vue|ts)$/.test(f) && !f.endsWith('.test.ts')) acc.push(f)
  }
  return acc
}
import { sessionStateKeys, gateStateKeys, codexToolStatusKeys } from './stateKeys'
describe('t() key references exist', () => {
  it('every literal t(\'...\') key is a locale leaf', () => {
    const keys = leafSet(zhTW)
    const files = walk(join(__dirname, '..'))
    const re = /(?:\bt|i18n\.global\.t)\(\s*['"]([\w.]+)['"]/g
    const missing: string[] = []
    for (const f of files) {
      const src = readFileSync(f, 'utf8')
      for (const m of src.matchAll(re)) if (!keys.has(m[1])) missing.push(`${m[1]} @ ${f}`)
    }
    expect(missing).toEqual([])
  })
  it('every state-map value is a locale leaf (maps referenced dynamically, not by literal t())', () => {
    const keys = leafSet(zhTW)
    const mapVals = [
      ...Object.values(sessionStateKeys), ...Object.values(gateStateKeys), ...Object.values(codexToolStatusKeys),
    ]
    expect(mapVals.filter(v => !keys.has(v))).toEqual([])
  })
})
```

- [ ] **Step 3: en render 測試（完整英文介面）**

```ts
// enRender.test.ts — 原本硬編中文的畫面在 locale=en 下轉英文
import { describe, it, expect } from 'vitest'
import { mountWithI18n } from '../test/i18n'
import GateConsole from '../components/GateConsole.vue'

describe('en locale renders English', () => {
  it('GateConsole degraded notice + empty are English under en', () => {
    const w = mountWithI18n(GateConsole, { props: { entries: [], decide: () => {}, degraded: true } }, 'en')
    expect(w.text()).toContain('degraded') // 英文，而非「核可記錄異常」
    expect(w.text()).not.toContain('核可記錄異常')
  })
})
```

- [ ] **Step 4: 驗證** — `npx vitest run src/i18n/` 全綠（parity、keyRefs、stateKeys、enRender、setup）。
- [ ] **Step 5: Commit** — `test(i18n): locale parity + literal key scan + en render`

---

## Task 10: 收尾 gate + inventory 掃描

**Files:** 無新 code；驗證。

- [ ] **Step 1: 人工 inventory 掃描** — grep 各元件殘留硬編 user-visible 字串（漏抽的補進 locale + 元件），確認除「不翻清單」外無殘留中文/英文硬編 UI 字串。
```bash
cd frontend && grep -rnoE '>[^<>{}]*[一-鿿][^<>{}]*<|placeholder="[^"]*[一-鿿][^"]*"|title="[^"]*[一-鿿][^"]*"' src/components src/App.vue | grep -v "t('"
# 預期：只剩不翻清單（provider/wire/欄位值/Gherkin/oracle 等）
```
- [ ] **Step 2: 前端全套** — `cd frontend && npx vitest run`（全綠）＋`npm run build`（vue-tsc 乾淨）。
- [ ] **Step 3: Wails build（桌面封裝整合）** — `wails build`（成功產出 app；驗相依＋main.ts plugin＋production bundle）。
- [ ] **Step 4: Commit（若有補漏）** — `chore(i18n): sweep residual hardcoded strings; final gate green`

---

## Self-Review（對照 spec）

- **§1 範圍/§2 架構** → Task 1（createI18n legacy:false、i18n.global export）、Task 7（store 入口）。
- **§3 key 命名** → Task 3 inventory（語意分層）。
- **§4 三映射＋passthrough** → Task 2、Task 6。
- **§5 interpolation／§7 8 操作×成敗＋按鈕/句中分開** → Task 3（success/failure/operationAction）、Task 4 Step 3。
- **§6 並列標籤** → Task 3（gate.label.*）、Task 5（GateConsole）。
- **§8 mountWithI18n／parity＋型別／literal scan／全狀態＋passthrough／en render／wails gate** → Task 8、Task 9、Task 10。
- **不翻清單** → Task 10 Step 1 掃描確認。

**Placeholder scan**：無 TBD；SettingsBar emit 確切來源於 Task 4 Step 3 由執行者定位（spec 已授權）。
**Type consistency**：`resolveState`/`sessionStateKeys`/`gateStateKeys`/`codexToolStatusKeys`/`mountWithI18n`/`t`/`i18n.global.t` 跨 task 命名一致；locale key 兩檔對齊由 Task 9 parity 守。
