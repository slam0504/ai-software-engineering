Feature: Spec／Plan 外部檔案變更的偵測與安全中止（A1b-1）

  # 對應 backlog A1 驗收條件 (5) 中屬於 A1b-1 的部分：偵測、背景安全重載、寫入預檢，
  # 以及「中止＋保留＋明確呈現原因」。reload／compare／保留本地的三選一屬 A1b-2，不在本檔。
  # 既有的 A1a 編輯閉環行為在 spec-plan-editing.feature，該檔的行為敘述不因本票改變。
  #
  # 依 design gate 裁定，本檔只放兩條使用者可見的行為 scenario：
  #   - C1（儲存等待期間重新聚焦）
  #   - 預檢等待期間回復原內容
  # C2／C3 的請求世代與回應失效細節屬實作層，留在元件測試，不在本檔。
  #
  # 「寫入基準」＝送出寫入時所帶的 expected_digest 來源，也是判斷檔案是否被外部改動的比較對象。
  # 「已儲存快照」沿用 spec-plan-editing.feature 的定義：最近一次成功寫入所送出的內容。

  Background:
    Given 工作區已選定一個納管的 spec 或 plan 檔
    And 該檔已載入編輯器，且編輯器內容、受控 buffer 與已儲存快照三者相同

  Scenario: 儲存等待期間重新聚焦，仍正確反映寫入結果
    Given 我使編輯器文件改變，該檔顯示為未儲存
    When 我按下儲存
    And 在寫入尚未返回前，視窗重新取得焦點
    Then 重新聚焦不會使這次寫入失效
    When 寫入成功返回
    Then 已儲存快照更新為這次送出的內容，寫入基準更新為新的值
    And 該檔不再顯示為未儲存
    And 不會因為重新聚焦而把畫面內容換成較舊的版本

  Scenario: 預檢等待期間回復原內容，外部衝突仍中止寫入且不自動重載
    Given 該檔在磁碟上已被其他來源改動
    And 我使編輯器文件改變，該檔顯示為未儲存
    When 我按下儲存
    And 在檢查尚未返回前，我把編輯器內容改回與已儲存快照相同，該檔不再顯示為未儲存
    And 檢查返回，磁碟內容與我持有的寫入基準不符
    Then 這次寫入被中止，內容不會被寫入磁碟
    And 系統明確告知中止原因是該檔已被外部改動
    And 編輯器內容、已儲存快照與寫入基準三者都不被更動
    And 系統不會因為當下沒有未儲存內容就自動重新載入該檔
