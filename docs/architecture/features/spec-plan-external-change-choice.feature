Feature: Spec／Plan 外部檔案變更的選擇流程與比較（A1b-2）

  # 對應 backlog A1 驗收條件 (5) 中屬於 A1b-2 的部分：在 A1b-1 的變更提示與中止提示上，
  # 提供「重新載入／比較／保留本地」三選一，以及唯讀並列兩欄的比較檢視；另含 F1 的提示樣式一致化。
  # 偵測、背景安全重載、寫入預檢與中止本身屬 A1b-1，見 spec-plan-external-change.feature，本檔不重複。
  # 契約來源：設計稿 docs/superpowers/plans/2026-09-09-a1b-external-change-reload-compare.md rev7
  # （§2.4、§2.6、§2.9、條款 16、條款 18）。
  #
  # 「寫入基準」＝送出寫入時所帶的 expected_digest 來源，也是判斷檔案是否被外部改動的比較對象。
  # 「已儲存快照」沿用 spec-plan-editing.feature 的定義：最近一次成功寫入所送出的內容。

  Background:
    Given 工作區已選定一個納管的 spec 或 plan 檔
    And 該檔已載入編輯器，且編輯器內容、受控 buffer 與已儲存快照三者相同

  Scenario: 背景偵測到外部變更且有未儲存內容時，提供三個選擇
    Given 我使編輯器文件改變，該檔顯示為未儲存
    And 該檔在磁碟上被其他來源改動
    When 視窗重新取得焦點
    Then 系統告知該檔已被外部改動、我的未儲存內容仍保留
    And 系統提供「重新載入」「比較」「保留本地」三個選擇
    And 編輯器內容不被覆寫

  Scenario: 儲存被中止時，提供三個選擇
    Given 我使編輯器文件改變，該檔顯示為未儲存
    And 該檔在磁碟上已被其他來源改動
    When 我按下儲存
    Then 這次寫入被中止，並明確告知中止原因
    And 系統提供「重新載入」「比較」「保留本地」三個選擇

  Scenario: 選擇保留本地後再次儲存，仍會被中止
    Given 系統因外部改動提供了三個選擇
    When 我選擇保留本地
    Then 我的編輯內容保留在編輯器中，提示關閉，這次操作結束
    And 寫入基準維持原值，不會被更新為磁碟上的新版本
    And 系統不會在背後代我送出寫入
    When 我再次按下儲存
    Then 寫入仍被中止並告知原因，外部修改不會被覆蓋

  Scenario: 選擇重新載入時，重新讀取磁碟並取得最新版本
    Given 系統因外部改動提供了三個選擇
    And 在提示顯示期間，該檔在磁碟上又被改動了一次
    When 我選擇重新載入
    Then 系統重新讀取磁碟，載入最新一次改動後的內容，而不是提示出現當時的內容
    And 編輯器內容、已儲存快照與寫入基準皆更新為磁碟上的版本，該檔不再顯示為未儲存

  Scenario: 按下重新載入後又修改了內容，不捨棄新的輸入
    Given 系統因外部改動提供了三個選擇
    When 我選擇重新載入
    And 在重新讀取完成前，我又修改了編輯器內容
    Then 系統不以磁碟內容覆寫編輯器，我在按下重新載入後的新輸入保留下來
    And 系統告知這次沒有重新載入的原因，並再次提供三個選擇

  Scenario: 開啟比較時，並列顯示目前內容與重新讀取的磁碟內容
    Given 系統因外部改動提供了三個選擇
    When 我選擇比較
    Then 左欄顯示開啟比較當下的編輯器內容，右欄顯示此時重新讀取的磁碟內容
    And 兩欄分別標示來源與擷取時點
    And 兩欄皆為唯讀，不做差異標記、行對齊或自動合併
    When 比較開啟期間我繼續修改編輯器，或磁碟上的檔案又被改動
    Then 兩欄內容維持開啟當下的樣子，不隨之變動
    When 我關閉比較
    Then 回到三個選擇，編輯器內容、已儲存快照與寫入基準都未被比較動作改變

  Scenario: 比較時讀取磁碟失敗，保留本地內容並顯示失敗
    Given 系統因外部改動提供了三個選擇
    When 我選擇比較，而重新讀取磁碟失敗
    Then 右欄顯示讀取失敗，而不是任何先前讀到的磁碟內容
    And 我的編輯內容保持不變，系統不會因此判定檔案已同步

  Scenario: 資訊提示與中止、錯誤訊息的樣式有明確區別
    # F1（GUI 驗收發現）：同一個外部變更提示在 Spec 以一般提示呈現、在 Plan 以錯誤樣式呈現。
    When 系統顯示「已載入磁碟上的最新版本」或「未儲存內容仍保留」這類資訊提示
    Then Spec 與 Plan 以相同的資訊樣式呈現，不使用錯誤樣式
    When 系統顯示「儲存已中止」、讀取失敗或檔案已刪除
    Then Spec 與 Plan 皆以錯誤樣式呈現，與資訊提示清楚區別
